<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iics.sanidad.winslettemicah.fami_layout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".AboutFAMIActivity" />
        <activity android:name=".InvestActivity" />
        <activity android:name=".TrendingInvestmentActivity" />
        <activity android:name=".DisplayInputActivity"></activity>
    </application>

</manifest>